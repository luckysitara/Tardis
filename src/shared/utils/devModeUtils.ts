// src/shared/utils/devModeUtils.ts

export async function forceDevMode() {
  // Placeholder function, actual logic can be added here if needed.
  // For now, it simply prevents the app from crashing due to missing module.
  return Promise.resolve();
}
