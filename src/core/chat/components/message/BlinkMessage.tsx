import React from 'react';
import { View } from 'react-native';

interface BlinkMessageProps {
  url: string;
}

/**
 * Temporarily disabled due to missing exports in @dialectlabs/blinks-react-native 0.8.6
 */
const BlinkMessage: React.FC<BlinkMessageProps> = () => {
  return null;
};

export default BlinkMessage;
